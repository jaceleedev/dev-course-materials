---
title: "숫자 맞추기 게임"
description: "반복문을 활용하여 컴퓨터와 두뇌 대결을 펼치는 완전한 숫자 맞추기 게임을 만들어보세요"
---

import InteractiveChecklist from "@/components/ui/interactive-checklist";

# 🎯 숫자 맞추기 게임

## 📋 프로젝트 개요

컴퓨터가 생각한 숫자를 맞추는 클래식한 게임을 반복문으로 구현해보세요. 단순한 맞추기부터 난이도 조절, 기록 관리까지 완전한 게임 시스템을 만들어봅시다!

## 🎯 학습 목표

- while문을 활용한 게임 루프 구현
- 조건부 반복과 사용자 입력 처리
- 게임 상태 관리와 피드백 시스템
- 난이도 조절과 통계 시스템 구현

## 🚀 기본 기능

### 핵심 요구사항

1. **숫자 맞추기 게임**

   - 컴퓨터가 1~100 사이 랜덤 숫자 선택
   - 사용자 추측 입력 받기
   - 업/다운 힌트 제공

2. **게임 진행**

   - 정답 맞출 때까지 반복
   - 시도 횟수 카운트
   - 최종 결과 표시

3. **입력 검증**
   - 1~100 범위 외 입력 처리
   - 잘못된 입력 형식 처리

## 🎨 추가 기능 1: 난이도 시스템

### 요구사항

- **쉬움**: 1~50, 시도 횟수 무제한
- **보통**: 1~100, 10회 제한
- **어려움**: 1~1000, 15회 제한
- **지옥**: 1~10000, 20회 제한

### 시도 횟수 제한

- 제한 횟수 초과 시 게임 오버
- 남은 횟수 실시간 표시
- 마지막 기회 경고 메시지

## 💡 추가 기능 2: 최고 기록 시스템

### 요구사항

- 난이도별 최소 시도 횟수 기록
- 게임 플레이 통계 (총 게임 수, 성공률)
- 연속 성공 기록 관리
- 평균 시도 횟수 계산

### 기록 표시

- 게임 종료 시 신기록 여부 알림
- 현재 기록과 비교 표시
- 전체 통계 조회 기능

## 🔥 추가 기능 3: 멀티플레이어 모드

### 요구사항

- **경쟁 모드**: 2명이 동시에 다른 숫자를 맞춰서 누가 더 빠른지 경쟁
- **협력 모드**: 2명이 힌트를 주고받으며 함께 숫자 맞추기
- **턴제 모드**: 번갈아가며 추측하고 상대방이 힌트 제공
- **토너먼트**: 여러 라운드로 구성된 토너먼트 시스템

### 게임 방식

- **실시간 경쟁**: 동시에 시작해서 누가 먼저 맞추는지 경쟁
- **점수 시스템**: 라운드별 승리 점수 누적
- **팀 기록**: 협력 모드에서 팀 최고 기록 관리

### 상호작용 기능

- **힌트 교환**: 플레이어 간 힌트 주고받기
- **응원 시스템**: 상대방 응원 또는 방해 메시지
- **실시간 현황**: 각 플레이어의 현재 상태 표시

## 💡 구현 힌트

### 필요한 파이썬 기능들

#### 1. 기본 게임 구조

```python
import random

# 게임 시작
print("🎯 숫자 맞추기 게임에 오신 것을 환영합니다!")

# 컴퓨터가 1~100 사이 숫자 선택
secret_number = random.randint(1, 100)
attempts = 0

print("🎲 컴퓨터가 1~100 사이의 숫자를 하나 생각했습니다.")
print("🎯 그 숫자를 맞춰보세요!")

while True:
    attempts += 1

    # 사용자 입력
    guess = int(input(f"🎲 {attempts}번째 시도 - 숫자를 입력하세요 (1-100): "))

    # 입력 검증
    if guess < 1 or guess > 100:
        print("❌ 1~100 사이의 숫자를 입력하세요!")
        attempts -= 1  # 잘못된 입력은 시도 횟수에서 제외
        continue

    # 정답 확인
    if guess == secret_number:
        print(f"🎉 축하합니다! {attempts}번 만에 정답을 맞췄습니다!")
        print(f"🏆 정답: {secret_number}")
        break
    elif guess < secret_number:
        print("📈 더 큰 숫자입니다!")
    else:
        print("📉 더 작은 숫자입니다!")

print("🎮 게임을 플레이해주셔서 감사합니다!")
```

#### 2. 난이도 선택 시스템

```python
# 난이도 선택
print("🎮 난이도를 선택하세요:")
print("1. 쉬움 (1~50, 무제한 시도)")
print("2. 보통 (1~100, 10회 시도)")
print("3. 어려움 (1~1000, 15회 시도)")

choice = int(input("선택 (1-3): "))

if choice == 1:
    min_num = 1
    max_num = 50
    max_attempts = 999  # 무제한
    difficulty = "쉬움"
elif choice == 2:
    min_num = 1
    max_num = 100
    max_attempts = 10
    difficulty = "보통"
elif choice == 3:
    min_num = 1
    max_num = 1000
    max_attempts = 15
    difficulty = "어려움"
else:
    print("❌ 잘못된 선택입니다!")

print(f"🎯 난이도: {difficulty}")
print(f"🎲 범위: {min_num}~{max_num}")
if max_attempts != 999:
    print(f"🎪 최대 시도 횟수: {max_attempts}회")

secret_number = random.randint(min_num, max_num)
attempts = 0

while attempts < max_attempts:
    attempts += 1

    # 남은 시도 횟수 표시
    if max_attempts != 999:
        remaining = max_attempts - attempts
        print(f"🎲 {attempts}번째 시도 (남은 기회: {remaining}회)")

    guess = int(input(f"숫자 입력 ({min_num}~{max_num}): "))

    if guess == secret_number:
        print(f"🎉 정답! {attempts}번 만에 맞췄습니다!")
        break
    elif guess < secret_number:
        print("📈 더 큰 숫자입니다!")
    else:
        print("📉 더 작은 숫자입니다!")

    # 마지막 기회 경고
    if max_attempts != 999 and attempts == max_attempts - 1:
        print("⚠️ 마지막 기회입니다!")

# 시도 횟수 초과 시 게임 오버
if attempts >= max_attempts:
    print(f"😢 게임 오버! 정답은 {secret_number}이었습니다.")
```

#### 3. 힌트 시스템

```python
# 거리 힌트 제공
difference = abs(guess - secret_number)
range_size = max_num - min_num

if difference <= range_size * 0.05:
    hint = "🔥 아주 가까워요!"
elif difference <= range_size * 0.1:
    hint = "🌡️ 가까워요!"
elif difference <= range_size * 0.2:
    hint = "🆗 보통이에요"
else:
    hint = "❄️ 멀어요"

if guess < secret_number:
    print(f"📈 더 큰 숫자입니다! ({hint})")
else:
    print(f"📉 더 작은 숫자입니다! ({hint})")
```

#### 4. 최고 기록 시스템

```python
# 최고 기록 저장 (난이도별)
best_easy = 999
best_normal = 999
best_hard = 999

# 게임 완료 후 기록 업데이트
if difficulty == "쉬움" and attempts < best_easy:
    best_easy = attempts
    print(f"🏆 쉬움 모드 신기록! {attempts}번")
elif difficulty == "보통" and attempts < best_normal:
    best_normal = attempts
    print(f"🏆 보통 모드 신기록! {attempts}번")
elif difficulty == "어려움" and attempts < best_hard:
    best_hard = attempts
    print(f"🏆 어려움 모드 신기록! {attempts}번")

# 현재 기록 표시
print(f"📊 현재 최고 기록:")
if best_easy != 999:
    print(f"  쉬움: {best_easy}번")
if best_normal != 999:
    print(f"  보통: {best_normal}번")
if best_hard != 999:
    print(f"  어려움: {best_hard}번")
```

#### 5. 입력 검증 시스템

```python
# 숫자 입력 검증
while True:
    try:
        guess = int(input(f"숫자 입력 ({min_num}~{max_num}): "))
        if min_num <= guess <= max_num:
            break
        else:
            print(f"❌ {min_num}~{max_num} 사이의 숫자를 입력하세요!")
    except ValueError:
        print("❌ 숫자만 입력하세요!")
```

#### 6. 연속 게임 시스템

```python
# 게임 통계
total_games = 0
total_attempts = 0
games_won = 0

while True:
    # 한 게임 진행
    total_games += 1

    # 게임 로직...

    if game_won:
        games_won += 1
        total_attempts += attempts

    # 다시 할지 묻기
    play_again = input("다시 하시겠습니까? (y/n): ")
    if play_again.lower() != 'y':
        break

# 최종 통계 출력
print(f"📊 게임 통계:")
print(f"🎮 총 게임 수: {total_games}")
print(f"🏆 성공한 게임: {games_won}")
if games_won > 0:
    success_rate = (games_won / total_games) * 100
    average_attempts = total_attempts / games_won
    print(f"📈 성공률: {success_rate:.1f}%")
    print(f"📊 평균 시도 횟수: {average_attempts:.1f}번")
```

#### 7. 멀티플레이어 시스템

```python
# 멀티플레이어 시스템 (기본 변수와 반복문만 사용)
print("🎮 멀티플레이어 숫자 맞추기 게임")

# 플레이어 등록
player1 = input("첫 번째 플레이어 이름: ")
player2 = input("두 번째 플레이어 이름: ")

# 플레이어 통계 변수들
player1_wins = 0
player1_total_games = 0
player1_best_attempts = 999
player1_total_attempts = 0

player2_wins = 0
player2_total_games = 0
player2_best_attempts = 999
player2_total_attempts = 0

print(f"🎮 플레이어 '{player1}'과 '{player2}'가 등록되었습니다!")

while True:
    print("\n🎮 멀티플레이어 메뉴")
    print("="*30)
    print("1. 경쟁 모드")
    print("2. 협력 모드")
    print("3. 미니 토너먼트")
    print("4. 리더보드")
    print("5. 종료")

    choice = input("선택하세요 (1-5): ")

    if choice == "1":
        # 경쟁 모드 - 2명이 번갈아 가며 같은 숫자 맞추기
        print(f"\n🏆 경쟁 모드: {player1} vs {player2}")

        # 난이도 설정
        difficulty = input("난이도 (쉬움/보통/어려움): ")
        if difficulty == "쉬움":
            min_num, max_num, max_attempts = 1, 50, 15
        elif difficulty == "보통":
            min_num, max_num, max_attempts = 1, 100, 12
        else:  # 어려움
            min_num, max_num, max_attempts = 1, 1000, 20

        # 컴퓨터가 숫자 선택 (간단한 방식)
        secret_number = (min_num + max_num) // 2 + (min_num % 7)  # 의사 랜덤

        attempts = 0
        current_player = player1
        winner = ""

        print(f"🎯 범위: {min_num}~{max_num}")
        print(f"🎪 최대 시도 횟수: {max_attempts}회")
        print("🚀 게임 시작!")

        while attempts < max_attempts and winner == "":
            attempts += 1

            print(f"\n--- 시도 {attempts}/{max_attempts} ---")
            print(f"🎮 {current_player}의 턴")

            guess = int(input(f"{current_player}의 추측: "))

            if guess == secret_number:
                print(f"🎉 {current_player} 승리! {attempts}번 만에 맞췄습니다!")
                winner = current_player

                # 승리 기록 업데이트
                if current_player == player1:
                    player1_wins += 1
                    player1_total_games += 1
                    player1_total_attempts += attempts
                    if attempts < player1_best_attempts:
                        player1_best_attempts = attempts
                    player2_total_games += 1
                else:
                    player2_wins += 1
                    player2_total_games += 1
                    player2_total_attempts += attempts
                    if attempts < player2_best_attempts:
                        player2_best_attempts = attempts
                    player1_total_games += 1

            elif guess < secret_number:
                print(f"📈 {current_player}: 더 큰 숫자!")
                # 플레이어 교대
                if current_player == player1:
                    current_player = player2
                else:
                    current_player = player1
            else:
                print(f"📉 {current_player}: 더 작은 숫자!")
                # 플레이어 교대
                if current_player == player1:
                    current_player = player2
                else:
                    current_player = player1

        if winner == "":
            print(f"\n😢 무승부! 정답은 {secret_number}이었습니다.")
            player1_total_games += 1
            player2_total_games += 1

    elif choice == "2":
        # 협력 모드 - 2명이 힌트를 주고받으며 함께 맞추기
        print(f"\n🤝 협력 모드: {player1} & {player2}")

        # 난이도 설정
        difficulty = input("난이도 (쉬움/보통/어려움): ")
        if difficulty == "쉬움":
            min_num, max_num, max_attempts = 1, 50, 20
        elif difficulty == "보통":
            min_num, max_num, max_attempts = 1, 100, 15
        else:  # 어려움
            min_num, max_num, max_attempts = 1, 1000, 25

        # 컴퓨터가 숫자 선택
        secret_number = (min_num + max_num) // 2 + (max_num % 13)

        attempts = 0
        current_player = player1
        other_player = player2
        team_success = False

        print(f"🎯 함께 맞출 숫자: {min_num}~{max_num}")
        print(f"🎪 최대 시도 횟수: {max_attempts}회")

        while attempts < max_attempts and not team_success:
            attempts += 1

            print(f"\n--- 시도 {attempts}/{max_attempts} ---")
            print(f"🎮 {current_player}의 턴")

            guess = int(input(f"{current_player}의 추측: "))

            if guess == secret_number:
                print(f"🎉 팀 승리! {attempts}번 만에 맞췄습니다!")
                print(f"🏆 {player1} & {player2} 팀워크 최고!")
                team_success = True

                # 두 플레이어 모두 승리 기록
                player1_wins += 1
                player1_total_games += 1
                player1_total_attempts += attempts
                if attempts < player1_best_attempts:
                    player1_best_attempts = attempts

                player2_wins += 1
                player2_total_games += 1
                player2_total_attempts += attempts
                if attempts < player2_best_attempts:
                    player2_best_attempts = attempts

            else:
                # 힌트 제공
                if guess < secret_number:
                    hint = "더 큰 숫자"
                else:
                    hint = "더 작은 숫자"

                print(f"💡 {other_player}의 힌트: {hint}!")

                # 추가 힌트 기회
                extra_hint = input(f"{other_player}, 추가 힌트를 주시겠습니까? (y/n): ")
                if extra_hint.lower() == 'y':
                    custom_hint = input("힌트 메시지: ")
                    print(f"💬 {other_player}: {custom_hint}")

            # 플레이어 교대
            if current_player == player1:
                current_player = player2
                other_player = player1
            else:
                current_player = player1
                other_player = player2

        if not team_success:
            print(f"\n😢 팀 실패! 정답은 {secret_number}이었습니다.")
            player1_total_games += 1
            player2_total_games += 1

    elif choice == "3":
        # 미니 토너먼트 (3라운드)
        print(f"\n🏆 미니 토너먼트 (3라운드)")
        print(f"👥 참가자: {player1} vs {player2}")

        tournament_player1_score = 0
        tournament_player2_score = 0

        for round_num in range(1, 4):
            print(f"\n🔥 라운드 {round_num}/3")

            # 간단한 숫자 맞추기 게임
            secret_number = 25 + round_num * 15  # 각 라운드마다 다른 숫자
            max_attempts = 8

            attempts = 0
            current_player = player1
            round_winner = ""

            print(f"🎯 범위: 1~100")
            print(f"🎪 최대 시도 횟수: {max_attempts}회")

            while attempts < max_attempts and round_winner == "":
                attempts += 1

                print(f"\n{current_player}의 {attempts}번째 시도:")
                guess = int(input(f"{current_player}의 추측: "))

                if guess == secret_number:
                    print(f"🎉 {current_player} 라운드 승리!")
                    round_winner = current_player

                    if current_player == player1:
                        tournament_player1_score += 1
                    else:
                        tournament_player2_score += 1

                elif guess < secret_number:
                    print("📈 더 큰 숫자!")
                    # 플레이어 교대
                    if current_player == player1:
                        current_player = player2
                    else:
                        current_player = player1
                else:
                    print("📉 더 작은 숫자!")
                    # 플레이어 교대
                    if current_player == player1:
                        current_player = player2
                    else:
                        current_player = player1

            if round_winner == "":
                print(f"😢 라운드 무승부! 정답: {secret_number}")

        # 최종 결과
        print(f"\n🏆 토너먼트 최종 결과:")
        print(f"📊 {player1}: {tournament_player1_score}승")
        print(f"📊 {player2}: {tournament_player2_score}승")

        if tournament_player1_score > tournament_player2_score:
            print(f"🥇 토너먼트 우승: {player1}")
        elif tournament_player2_score > tournament_player1_score:
            print(f"🥇 토너먼트 우승: {player2}")
        else:
            print("🤝 토너먼트 무승부!")

    elif choice == "4":
        # 리더보드 표시
        print("\n🏆 리더보드")
        print("="*40)

        # 플레이어 1 통계
        if player1_total_games > 0:
            player1_win_rate = (player1_wins * 100) // player1_total_games
            player1_avg_attempts = player1_total_attempts // player1_wins if player1_wins > 0 else 0
        else:
            player1_win_rate = 0
            player1_avg_attempts = 0

        # 플레이어 2 통계
        if player2_total_games > 0:
            player2_win_rate = (player2_wins * 100) // player2_total_games
            player2_avg_attempts = player2_total_attempts // player2_wins if player2_wins > 0 else 0
        else:
            player2_win_rate = 0
            player2_avg_attempts = 0

        # 순위 결정 (승수 기준)
        if player1_wins > player2_wins:
            print(f"🥇 1위: {player1}")
            print(f"   승률: {player1_win_rate}% ({player1_wins}/{player1_total_games})")
            print(f"   최고 기록: {player1_best_attempts}번")
            print(f"   평균 시도: {player1_avg_attempts}번")
            print()
            print(f"🥈 2위: {player2}")
            print(f"   승률: {player2_win_rate}% ({player2_wins}/{player2_total_games})")
            print(f"   최고 기록: {player2_best_attempts}번")
            print(f"   평균 시도: {player2_avg_attempts}번")
        elif player2_wins > player1_wins:
            print(f"🥇 1위: {player2}")
            print(f"   승률: {player2_win_rate}% ({player2_wins}/{player2_total_games})")
            print(f"   최고 기록: {player2_best_attempts}번")
            print(f"   평균 시도: {player2_avg_attempts}번")
            print()
            print(f"🥈 2위: {player1}")
            print(f"   승률: {player1_win_rate}% ({player1_wins}/{player1_total_games})")
            print(f"   최고 기록: {player1_best_attempts}번")
            print(f"   평균 시도: {player1_avg_attempts}번")
        else:
            print("🤝 동점!")
            print(f"📊 {player1}: {player1_wins}승 ({player1_win_rate}%)")
            print(f"📊 {player2}: {player2_wins}승 ({player2_win_rate}%)")

    elif choice == "5":
        print("👋 게임을 종료합니다!")
        break
    else:
        print("❌ 1~5 중에서 선택하세요!")
```

### 게임 설계 팁

- **난이도 조절**: 범위와 시도 횟수로 난이도 조정
- **힌트 시스템**: 거리에 따른 다양한 힌트 제공
- **사용자 경험**: 명확한 안내 메시지와 피드백
- **기록 관리**: 최고 기록과 통계로 재미 증대

### 도전 과제

1. **특별 모드**: 홀수/짝수만 사용하는 모드
2. **시간 제한**: 각 시도마다 시간 제한 추가
3. **협력 모드**: 두 명이 함께 맞추는 모드
4. **AI 학습**: 컴퓨터가 플레이어 패턴 학습

## 📝 완성 체크리스트

<InteractiveChecklist
  storageKey="number-guessing-game-checklist"
  title="🎯 숫자 맞추기 게임 체크리스트"
  items={[
    {
      id: "basic-game",
      text: "while문으로 기본 숫자 맞추기 게임을 구현한다",
    },
    {
      id: "hints",
      text: "업/다운 힌트를 정확히 제공한다",
    },
    {
      id: "input-validation",
      text: "잘못된 입력에 대한 검증과 재입력을 처리한다",
    },
    {
      id: "difficulty-levels",
      text: "4가지 난이도를 선택할 수 있다",
    },
    {
      id: "attempt-limit",
      text: "시도 횟수 제한과 게임 오버 처리가 작동한다",
    },
    {
      id: "record-system",
      text: "최고 기록 관리 시스템이 있다",
    },
    {
      id: "statistics",
      text: "게임 통계 (성공률, 평균 등)를 계산하고 표시한다",
    },
    {
      id: "competitive-mode",
      text: "2명이 번갈아가며 경쟁하는 멀티플레이어 모드를 구현한다",
    },
    {
      id: "cooperative-mode",
      text: "2명이 힌트를 주고받으며 협력하는 모드를 구현한다",
    },
    {
      id: "tournament-system",
      text: "3라운드 미니 토너먼트 시스템을 구현한다",
    },
    {
      id: "leaderboard",
      text: "기본 변수로 플레이어별 승률과 기록을 관리하는 리더보드가 있다",
    },
    {
      id: "menu-system",
      text: "완전한 메뉴 시스템으로 모든 기능에 접근할 수 있다",
    },
  ]}
  completionMessage="🎉 축하합니다! 완전한 숫자 맞추기 게임 마스터가 되었습니다!"
/>

## 🎉 완성 후 해볼 것

완성된 게임으로 친구들과 누가 더 적은 시도로 맞추는지 경쟁해보세요! 컴퓨터가 맞추는 게임에서는 거짓 힌트를 주면 어떻게 되는지도 실험해보세요!
